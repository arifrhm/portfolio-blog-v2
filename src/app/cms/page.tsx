import React from 'react';
import CMSApp from '@/components/CMSApp'; // Adjust the path as necessary

const CMSPage: React.FC = () => {
  return (
    <div>
      <CMSApp />
    </div>
  );
};

export default CMSPage;
